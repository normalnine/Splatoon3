using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KDH_Destroy : MonoBehaviour
{
    // Start is called before the first frame update
    void VFX_Destroy()
    {
        Destroy(gameObject);
    }
}
